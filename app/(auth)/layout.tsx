const Layout = ({ children }: { children: React.ReactNode }) => {
  return <div className="flex-center min-h-screen bg-blue-50 bg bg-dotted-pattern bg-cover bg-fixed bg-center w-full"
  >{children}</div>;
};
export default Layout;
